[package]
name = "clamav-stream"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio-stream = "0.1.14"
bytes = "1"
pin-project = "1"

[dev-dependencies]
tokio = { version = "1", features = ["fs", "macros", "rt-multi-thread"] }
tokio-util = { version = "0.7", features = ["io"] }
